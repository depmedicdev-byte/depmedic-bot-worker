{
  "name": "depmedic-bot-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ci-doctor": "node tools/build-ci-doctor-esm.mjs",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
